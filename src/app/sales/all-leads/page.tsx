"use client";

import SalesLeadsPage from "../leads/page";

/** Reuses the sales-leads list so both sidebar entries show the same live lead data. */
export default function AllLeadsPage() {
  return <SalesLeadsPage />;
}
